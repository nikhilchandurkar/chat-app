version: "3.9"

services:
  react-app:
    image: node:18-slim
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    environment:
      - CHOKIDAR_USEPOLLING=true  # Fixes file watching in Docker on some OS
